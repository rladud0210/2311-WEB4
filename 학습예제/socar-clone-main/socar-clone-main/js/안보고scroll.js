// advatage를 불러오고
// 반복문을 활용해서 advantage와 advantage의 자식요소에게 scroll-inter 를 넣는다.

const advantage = document.getElementsByClassName('advantage')

for(i = 0 ; i = advantage.length ; i++){
    for(j = 0 ; j = advantage[0].childElementCount ; )
}